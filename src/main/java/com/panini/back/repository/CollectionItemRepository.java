package com.panini.back.repository; import com.panini.back.entity.CollectionItem; import org.springframework.data.jpa.repository.JpaRepository; import java.util.List; import java.util.Optional; public interface CollectionItemRepository extends JpaRepository<CollectionItem,Long>{ List<CollectionItem> findByUser_Username(String username); Optional<CollectionItem> findByUser_UsernameAndSticker_Code(String username, String stickerCode); }
